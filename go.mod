// El path apunta a kampe-ir, que TODAVIA NO RESUELVE: el slug del repo sigue
// siendo 'correai' hasta que Abraham haga el rename en Settings de GitHub.
// No rompe el build porque no hay dependencias externas ni imports remotos,
// pero eso queda NO MEDIDO hasta que lo diga el CI.
module github.com/gatehot59-star/kampe-ir

go 1.22
